# Copy to .env.local for local development.
# Base URL of the FastAPI backend. Must be reachable from the *browser*,
# because the seat map is fetched client-side.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
